<UserControl x:Class="AvonManager.Controls.FileLoader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400" Loaded="UserControl_Loaded" Unloaded="UserControl_Unloaded">
    
    <StackPanel x:Name="LayoutRoot" Background="White" Orientation="Horizontal">
        <Button Margin="3" Padding="10,2" Name="openFileDialog" Click="openFileDialog_Click" ToolTipService.ToolTip="Datei laden">
            <TextBlock Text="T" />
        </Button>
        <Button Margin="3" Padding="10,2" Name="capturePicFromWebcam" ToolTipService.ToolTip="Webcam" Click="capturePicFromWebcam_Click">
            <TextBlock Text="V" />
        </Button>
        <Button Margin="3" Padding="10,2" Name="deletePic" ToolTipService.ToolTip="Bild löschen" Click="deletePic_Click">
            <Image Height="20" Source="/AvonManager;component/Assets/Images/Delete.PNG" Stretch="UniformToFill" />
        </Button>
    </StackPanel>
</UserControl>
